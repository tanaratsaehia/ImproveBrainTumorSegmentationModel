mlflow runner:
    cd KKU-World/year_3/MLOps/mlflow_data
    conda activate mlops_final
    OPTION
        nohup mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri ./mlruns --serve-artifacts > mlflow.log 2>&1 &

find pid
    ps aux | grep mlflow
    sudo pkill -9 -f mlflow